-- Select specific fields from the given tables
-- Here we specify the fields by comma
-- And this is the standard practice to select the fields with comma instead of selecting them with '*'.
-- Because select all the fields using * will degrade the performance.
SELECT id, first_name FROM customers;
